# Copy to .env for local overrides (rarely needed — localhost:4545 is the default).
# On Vercel, set this in Project Settings → Environment Variables to your
# deployed backend's URL (e.g. https://mimic-backend.up.railway.app/api).
VITE_API_BASE=http://localhost:4545/api
